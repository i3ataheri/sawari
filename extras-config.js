// ============================================================
//  تنظیمات افزونه‌ها (واترمارک)
//  برای فعال/غیرفعال هر کدام، مقدار `enabled` را true یا false کنید.
//  نیازی به دست زدن به index.html یا data.js نیست.
// ============================================================

const EXTRAS = {

    // ---------- واترمارک حرفهای (تکتکی، کج، پراکنده) روی همه صفحات ----------
    watermark: {
        enabled: true,                 // true = نمایش، false = حذف
        phone: "0548471318",           // شماره‌ای که تکرار می‌شود
        opacity: 0.12,                 // شفافیت (0 تا 1) — کمرنگ و محو
        fontSize: 46,                  // سایز فونت واترمارک به‌پیکسل
        color: "#d4af37",              // رنگ (طلایی)
        rotation: -26,                 // چرخش به‌درجه (منفی = کج)
        spacingX: 620,                 // فاصله افقی بین تکرارها (بزرگ=تکرار کم)
        spacingY: 420,                 // فاصله عمودی بین ردیف‌ها
        stagger: true                  // جابه‌جایی یک‌درمیان ردیف‌ها (نقش ضربدری)
    }
};